//  Utils.h

#include <iostream>

//  Use once.
#pragma once

#ifndef _UTILS_H_
#define _UTILS_H_

void arrayPrint(int[], size_t);
int sumArray(int[], size_t);
int largestValue(int[], size_t);
int findValue(int[], size_t,int,int);
int countElement(int[], size_t, int, int);
void arrayReversal(int[], size_t);
bool uniqueArrayCheck(int[], size_t);

#endif
